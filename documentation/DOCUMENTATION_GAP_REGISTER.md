# Documentation Gap Register

This register distinguishes missing documents from unimplemented systems. It is not a feature roadmap.

## Added in the current documentation pass

- AI, data, and event/networking architecture records
- Architecture, entity/content, and system indexes
- Commands and configuration references
- Testing guide

## Planned-system documents not yet created

The project index names several architecture documents whose systems are not yet established in the current codebase: HiveCore intelligence, task assignment, memory, broader AI planning, and platform-level reusable theory. Create those documents when a design is approved or a specification is ready; creating detailed “current” documentation now would imply implementation that does not exist.

## Layout reconciliation pending

`PROJECT_INDEX.md` describes an intended numbered `docs/` hierarchy, while this repository uses `documentation/` with unnumbered category folders. This is a documentation-organization decision, not a safe automatic rename. Preserve the current layout until a migration plan is approved.

